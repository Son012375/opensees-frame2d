# 2026-04-16~17 작업 기록

## 완료 작업

### 높음 우선순위 (4개)

#### 1. BMD/SFD/Axial 스케일 슬라이더
- Diagram 패널에 0.1x~10x 로그 슬라이더 추가
- `_diagramScaleMultiplier` 전역 변수 + `_buildDiagrams()`에 곱해짐
- Diagram Off 시 슬라이더 자동 숨김, Auto(Reset) 버튼

#### 2. Excel Export (백엔드 다중 시트, Wide Pivot)
- `pandas` + `openpyxl` 추가 (`requirements.txt`)
- `/api/export/excel/{job_id}` 엔드포인트 신규
- 5개 시트: Displacements / Member_Forces / Reactions / Envelope / Metadata
- Wide Pivot: 행=노드/부재, 열=(Info) + (case x metric) MultiIndex
- 케이스 그룹별 헤더 색상 (DL=시안, LL=주황, EQ=빨강, Wind=보라, RSA=파랑, Combo=교차 연녹)
- openpyxl 직접 작성 (pandas MultiIndex+index=False 제약 회피)
- 부재력 배열 → signed peak 스칼라화, MergedCell column_letter 회피
- freeze pane A3 (2행 헤더 고정)

#### 3. Node/Member 번호 토글
- Filter 패널 하단에 `Node #`, `Member #` 분리 체크박스
- 캔버스 100x28 스프라이트, Node=녹색(#2e7d32), Member=보라(#6a1b9a)
- 모델 재빌드 시 `_refreshNumberLabels` 후크로 동기화

#### 4. 층별 단면 일괄 변경
- Modify Section에 Story 체크박스 영역 + [All/None/이 부재 층] 퀵 버튼
- [전체 적용] / [선택 층] / [이 부재만] 3버튼
- 하위 층 기준 (`Math.min(ni.story, nj.story)`)
- 체크 시 주황 프리뷰 하이라이트 (wire + solid mesh 양쪽)

### 중간 우선순위 (2개)

#### 5. Story Drift 차트 탭
- 3D Viewer 상단 `[3D Model] [Story Drift]` 탭 버튼
- 3D canvas 60% + drift 패널 40% flex 분할
- 수평 bar chart (Canvas 2D, DPR=2 고해상도)
- 어두운 배경 + 흰색 라벨 + OK=초록/NG=빨강 바
- KDS 허용치 라인 (주황 점선 + 배경 박스 라벨)
- X축 눈금 1/N 형식
- 케이스별 자동 허용치 전환: EQ→1/50(내진), DL/LL/Wind→1/200(사용성)
- Allowable Drift 드롭다운 수동 오버라이드 (1/200, 1/50, 1/67, 1/100, 1/300)

#### 6. IFC 단면 매핑 확장
- `_parse_revit_profile_name`: SHS/RHS/CHS/PFC/TFC/L/H 6종 패턴
- `_resolve_profile_by_parsed`: Supabase DB 범용 조회
- `_extract_profile_from_item`: 범용 파서 연결
- `ifc_parser_v2.py`: db_name 우선 반환
- Solid Section Shape: SHS(사각 중공), RHS, CHS(원형 중공), L(ㄱ자), 채널(ㄷ자)
- Hover tooltip: 단면 타입별 필드 분기 (SHS→B,t / CHS→D,t / H→H,B)
- API: section_type + t_mm 필드 추가
- Solid/Wire 전환 시 mesh 동기화 (scene.traverse + memberMeshes + solidMeshMap)
- 선택 하이라이트 solid mesh 동기화
- Story 필터 solid mesh 동기화

### 기타 수정
- 반력 수치 Values 토글 연동 (`_reactionLabelSprites`)

---

## 검증 완료

| 항목 | 방법 | 결과 |
|------|------|------|
| SHS IFC 파싱 | Revit IFC4 (SHS_test.ifc) | 기둥 □-150x150x5.0, 보 □-100x100x4.0 |
| Solid Section SHS | 에디터 | 사각 중공 정상 |
| Solid Section CHS | 에디터 드롭다운 | 원형 중공 정상 |
| Solid Section L | 에디터 드롭다운 | ㄱ자 정상 |
| Excel Export | 다운로드 검증 | 5시트, 색상, 값 정상 |
| Story Drift 차트 | 에디터 | 수평 bar, 허용치 전환 정상 |
| 층별 단면 변경 | 에디터 | 프리뷰+적용 정상 |

## IFC 파싱 추가 검증 필요 (Revit IFC 미제작)
- CHS, L, 채널, RHS — ProfileName 패턴 미확인

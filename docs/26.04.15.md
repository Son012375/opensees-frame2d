# 2026-04-15 작업 기록

## 오늘 완료한 작업

### V2 UI 대규모 개선 (4개 커밋)

#### 1. Display Filter + 하중 화살표 + SFD/BMD
- Display Filter 패널: Loads(DL/LL/EQ/Wind), Story, Member Type 토글
- 하중 화살표: 분포하중(보 위 배열) + 수평하중(층 중심) + 색상 분리
- SFD/BMD/Axial 3D 다이어그램: 폴리곤 면 + 외곽선 + 최대값 라벨
- 개별 부재 Canvas 다이어그램 (N/V/M × 3)
- beam_y 부재력 수정: OpenSees 로컬 T_kNm = 실제 강축 휨
- Properties 탭 정리: Results / Modal / DC (기본=DC)

#### 2. 보-보 교차점 분할 + 프로젝트 저장/불러오기 등 7개 기능
- 보-보 교차점 자동 분할 (Python + JS, 2D line intersection)
- 프로젝트 저장/불러오기 (.v2proj, 해석 결과 포함)
- 에디터 Deformed shape + 스케일 슬라이더
- 개별 부재 단면 변경 ("이 부재만")
- 복사/미러/층 복사 (플로팅 패널)
- 반력 시각화 (지점 화살표 + kN 수치)
- 부재력 hover tooltip (N/V/M)

#### 3. BMD/반력/Copy 패널 등 UI 세부 수정
- Deformed shape 명시적 체크박스 토글
- displacements 데이터 전달 수정 (convertV2ResultToV1 버그)
- Copy/Mirror 플로팅 드래그 패널 (prompt() 제거)
- Copy 도구 모드 (편집 툴바 버튼)
- 노드 mesh 복사 제외 (type !== 'node')
- Story 선택 수정 (Base=지점노드만)
- BMD 라벨: i-end/j-end + 고점 + Values 토글
- 반력: 최소 화살표 보장 + 스케일 조정 + 수치 라벨 고해상도
- updated_model 동기화 (split 후 → JS)
- 해석 결과 포함 저장 (version 3, 재해석 불필요)

---

## 현재 시스템 기능 총정리

### 파이프라인
```
[IFC 업로드] → [자동 모델링] → [자동 하중(KDS)] → [해석(OpenSees)] → [설계검토(KDS/AISC)] → [리포트]
     ✅            ✅              ✅                ✅                ✅                    ✅
```

### Midas Gen 대비 우위
- IFC → 구조모델 자동 변환
- KDS 자동 하중 생성 (DL/LL/EQ/Wind)
- 18개 하중조합 자동 생성
- 자연어 → 해석 Config
- 프로젝트 파일 (.v2proj, 해석 결과 포함)

### 동등 기능
- 3D 정적/모달 해석
- RSA (CQC/SRSS)
- SFD/BMD/Axial 다이어그램
- 하중/반력 시각화
- Deformed shape + 스케일
- Design Check (KDS drift + AISC member)
- 복사/미러/층 복사

---

## 남은 작업 (우선순위 순)

### 높음
1. BMD 스케일 조절 슬라이더
2. Export (CSV/Excel) — 부재력, 변위, 반력
3. 부재/절점 번호 표시 토글
4. 층별 단면 일괄 변경 UI

### 중간
5. IFC 단면 매핑 확장 (Box/Pipe/C채널)
6. 슬래브 하중 분배 개선 (tributary → 2-way)
7. 다중 재료 지원 (RC + Steel)
8. 에디터 층간변위 그래프

### 낮음
9. 하중 수치 라벨
10. 단면 프리뷰 도면
11. Ctrl+Z 해석 후 undo
12. DXF/STEP 내보내기

---

## 평가 요약

> IFC→자동하중→해석→검토 파이프라인은 Midas를 넘어서는 수준.
> 실무 신뢰성(슬래브 분배, 다양한 단면)과 데이터 추출(Export)이 보완되면 독립 상용 도구 수준에 근접.

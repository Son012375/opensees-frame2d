# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
.pytest_cache/

# Virtual environments
venv/
ENV/
env/
.venv/
opensees-mcp/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Environment / Secrets
.env
.env.local

# Jobs data (generated)
webapp/backend/jobs/

# Docker
*.log

# OS
.DS_Store
Thumbs.db
nul
NUL
CON
PRN
AUX

# Test outputs (but keep templates)
*.html
!webapp/backend/templates/*.html
!webapp/backend/templates/**/*.html

# The landing page is source and shipped artifact, not test output. index.html
# is generated by scripts/build_landing.py, but it is tracked anyway for the
# same reason data/demo_bundle/*.json is: `/` serves it directly, so a deploy
# that skips the build step would silently fall back to the bare editor — the
# problem the landing exists to fix.
!landing/index.template.html
!landing/index.html
!landing/files/*.html

# Large data — KDS PDF images (derived from PDFs)
data/kds_images/

# Runtime KDS evidence audit log (chat_audit_log.py default path).
# Contains evidence quotes + member ratios + analysis_id — sensitive,
# never commit. Override location via CHAT_AUDIT_LOG_PATH.
data/chat_audit/

# Runtime §10 narration provenance log (narration_audit.py default path).
# AI-drafted prose + facts snapshot per analysis — override via NARRATION_AUDIT_LOG_PATH.
data/narration_audit/

# Runtime §10 narration facts-hash cache (narration_cache.py default path, C2′ 결정성).
# Validated LLM candidates keyed by facts+prompt_hash — override via NARRATION_CACHE_DIR.
data/narration_cache/

# Generated test output HTML reports
mcp-server/test_output/
mcp-server/*.html
mcp-server/*.png

# Claude local settings
.claude/settings.local.json
.claude/skills/

# Binary / presentation files
*.pptx
*.docx

# Local DXF samples / one-off scripts (not part of repo)
test_*.dxf
make_script_*.py
mdpdf.log

# Legacy directories (superseded by mcp-server + webapp)
/adapters/
/agents/
/apply_mvp/
/core/
/pipeline/
/streamlit_app/
/utils/
/migrations/
/opensees-ai-agent/
gpt용 md/

# Legacy files (local notes/scripts, not part of build)
opensees-mcp-copy.md
openseespy_commands_reference.md
test_ollama_opensees.py
test_opensees.py
# NOTE: conftest.py is required for pytest collection — keep tracked.
kds_pipeline.py

# Generated IFC test fixtures — rebuilt on demand by tests/fixtures/minimal_ifc.py.
tests/fixtures/*.ifc
# render.yaml, runtime.txt, .python-version → tracked (Render deploy config)

# Voyage live-smoke artifacts — embedded vectors + dev script.
# NARROW patterns only — never widen to data/*.jsonl (would catch
# small test fixtures we may need to commit later).
data/kds_sample_index.jsonl
data/kds_index.jsonl
scripts/smoke_voyage_explain.py
.codex_tmp/
